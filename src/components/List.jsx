var React = require("react");
var ListItem = require("./ListItem.jsx");

var List = React.createClass({
    render: function() {
      return (
        <ul>        
        </ul>
      );
    }
});

module.exports = List;
